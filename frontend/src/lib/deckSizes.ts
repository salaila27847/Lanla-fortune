// Mirrors the backend decks — must stay in sync.
// Tarot: standard 78-card RWS deck
// (backend/app/knowledge_base/tarot/{major,minor}_arcana.yaml).
export const TAROT_DECK_SIZE = 78;

// Oracle: "ลานลาออราเคิล" (lanla_original) deck — 4 themes x 22 cards
// (backend/app/knowledge_base/oracle/decks/lanla_original/).
export const ORACLE_DECK_SIZE = 88;
